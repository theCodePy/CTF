TCP null scan didn't work all ports are in ignore state

open ports found :
	PORT     STATE SERVICE VERSION
	21/tcp   open  ftp     vsftpd 3.0.3
	80/tcp   open  http    Apache httpd 2.4.18 ((Ubuntu))
	2222/tcp open  ssh     OpenSSH 7.2p2 Ubuntu 4ubuntu2.8 (Ubuntu Linux; protocol 2.0)


--Script-vuln:
	didn't find anything interesting.

robots.txt:
	found interesting information in robots.txt


the application running on porn 80 is CMS Made simple version  2.2.8 which is vulnerable to SQLi
CVE-2019-9053
Exploit : https://www.exploit-db.com/exploits/46635

directory Bruteforce on website:
found directory /simple

# holding further testing for now. 
# webserver doesn't responding....
# I will use: python3 46635 -u http://ip_address/simple --crack -w rockyoutxt
# to exploit sqli on this later.
